# 测试领域与评审文档

目标源码：`https://github.com/nervosnetwork/ckb-explorer.git`，当前分析版本 `develop@0495ecd00a839f7618bad752f5ad92071124a991`。
执行方式：兼容性套件比较基准与候选环境的 HTTP 行为；RPC 正确性套件把 Explorer API 结果与同一网络、同一区块高度的 CKB RPC 结果或可验证推导值比较。机器可执行的路由与 fixture 保存在各套件配置中，不在评审文档重复维护。

| 测试领域 | 责任与边界 | 入口 | 可观察结果 | 评审文档 |
| --- | --- | --- | --- | --- |
| HTTP API 通用契约 | 比较所有接口共有的路由、媒体类型、错误、分页、CSV、缓存和差异报告规则；不判断具体业务数据是否正确 | `/api/v1/*`、`/api/v2/*`、基准与候选 URL | 状态、选定响应头、数据类型和值、顺序、分页、逐字段差异 | `suites/ckb-explorer-api-rpc-compatibility/reviews/http-api-contract.md` |
| V1 区块列表 RPC 正确性 | 在公开主网和测试网分别以 Explorer 返回的区块高度为锚点，与同网络 CKB RPC 核对区块身份及可直接验证的列表字段，并单独观测同步高度差；不跨网络比较，也不评审双 Explorer 环境兼容性、通用 HTTP 契约、分页和排序 | 主网与测试网的 `GET /api/v1/blocks`、`GET /api/v1/blocks/:height`，以及同网络 CKB RPC `get_tip_header`、`get_block_by_number` 与 `get_block_economic_state` | 每个网络内的 API/RPC 链身份、同步高度差、区块高度、哈希、时间戳、交易数量，以及派生字段的计算证据与差异 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-blocks-index.md` |
| V1 区块详情 RPC 正确性 | 在公开主网和测试网分别按区块高度与区块哈希查询同一规范链区块，并用同网络 CKB RPC 及可验证推导值核对详情字段；不跨网络比较，也不评审双 Explorer 环境兼容性、通用 HTTP 契约和区块交易列表 | 主网与测试网的 `GET /api/v1/blocks/:id`，以及同网络 CKB RPC `get_block_by_number`、`get_block`、`get_block_economic_state` 与必要的链参数查询 | 高度与哈希两种查询的一致性、区块头与 Epoch 字段、交易/提案/叔块计数及根哈希、容量/费用/奖励、矿工信息、区块大小与 cycles 等可核对详情值及差异 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-blocks-show.md` |
| V1 区块 CSV RPC 正确性 | 在公开主网和测试网分别下载区块 CSV，并用同网络 CKB RPC 核对默认及过滤后的区块集合、顺序、500 行上限和可由链数据推导的列值；不跨网络比较，也不评审双 Explorer 环境兼容性、通用下载响应头、错误契约和 CSV 转义规范 | 主网与测试网的 `GET /api/v1/blocks/download_csv` 及其 `start_date`、`end_date`、`start_number`、`end_number` 过滤条件，以及同网络 CKB RPC `get_block_by_number`、`get_block_economic_state` | CSV 表头和列映射、筛选边界、区块高度顺序与行数上限、交易数量、Unix 时间戳、CKB 奖励、矿工地址和 UTC 日期及差异 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-blocks-download-csv.md` |
| V1 区块交易列表 RPC 正确性 | 在公开主网和测试网分别以规范链区块哈希查询 Explorer 交易列表，并用同网络 CKB RPC 核对交易归属、顺序和可由链数据推导的摘要字段；不跨网络比较，也不评审双 Explorer 环境兼容性、通用 HTTP 契约、分页规则、创建时间以及 RGB/Bitcoin 索引注解 | 主网与测试网的 `GET /api/v1/block_transactions/:id`（`:id` 为区块哈希）及其 `tx_hash`、`address_hash` 过滤条件，以及同网络 CKB RPC `get_block`、`get_transaction` | 区块内交易集合与链上顺序、交易哈希、Cellbase 标识、区块高度与时间戳、输入输出计数和预览，以及按交易哈希或链上地址过滤后的成员关系及差异 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-block-transactions-show.md` |
| V1 交易列表 RPC 正确性 | 在公开主网和测试网分别取得近期已提交的普通交易简表，并用同网络 CKB RPC 核对默认列表成员、时间与区块内顺序以及五个可由链数据推导的字段；不跨网络比较，也不评审双 Explorer 环境兼容性、通用 HTTP 契约、显式分页和排序、交易详情、条件查询、待处理交易及协议扩展注解 | 主网与测试网不带 `page`、`page_size` 的 `GET /api/v1/transactions`，以及同网络 CKB RPC `get_tip_header`、`get_block_by_number`、`get_block`、`get_transaction` 和输入引用的上一笔交易 | 最新普通已提交交易的集合与顺序、交易哈希、区块高度与时间戳、输入容量总和、输入输出数量差及逐字段差异 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-transactions-index.md` |
| V1 交易详情 RPC 正确性 | 在公开主网和测试网分别按已提交普通交易及 Cellbase 交易哈希查询详情，并用同网络 CKB RPC 核对交易原始结构、区块归属和可由链数据推导的详情字段；覆盖是否展示 Cells 的调用方开关，不跨网络比较，也不评审双 Explorer 环境兼容性、通用 HTTP 与错误契约、待处理或拒绝状态、全局与 Epoch 极值以及 RGB/Bitcoin/UDT/DAO 等协议扩展注解 | 主网与测试网的 `GET /api/v1/transactions/:id` 及可选 `display_cells`，以及同网络 CKB RPC `get_transaction`、`get_block` 和输入引用的上一笔交易 | 交易哈希与提交状态、普通交易与 Cellbase 标识、版本、witnesses、Cell/Header Dependencies、区块高度与时间戳、输入输出成员和顺序、容量与脚本等可推导展示值、交易费、序列化大小、cycles、Cells 展示开关及逐字段差异 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-transactions-show.md` |
| V1 交易条件查询 RPC 正确性 | 在公开主网和测试网分别按 CKB 地址查询已提交交易，并用同网络 CKB Indexer 与节点 RPC 核对过滤成员、链上顺序及列表详情；同时核对省略地址时的全局查询分支，不跨网络比较，也不评审双 Explorer 环境兼容性、通用 HTTP 与分页错误契约、数据库创建时间及 RGB/Bitcoin/UDT/DAO 等协议扩展注解 | 主网与测试网的 `POST /api/v1/transactions/query`，JSON 参数 `address`、`page`、`page_size`，以及同网络 CKB Indexer `get_transactions` 和节点 RPC `get_transaction`、`get_block`、输入引用的上一笔交易 | 地址参与交易的去重集合与顺序、分页成员及总数、交易哈希和区块字段、输入输出总数与前 10 项预览、查询地址净容量变化 `income`、省略地址时的最近普通交易及逐字段差异 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-transactions-query.md` |
| V1 Cell Input Lock Script RPC 正确性 | 核对普通输入引用输出的 Lock Script、输入关联、多输入隔离、Cellbase 与 ID 边界；不评审通用 HTTP 契约和 Explorer 脚本元数据 | 主网与测试网的 `GET /api/v1/cell_input_lock_scripts/:id`，以及同网络 CKB RPC `get_transaction` | RPC 引用输出 Lock Script 的 `code_hash`、`hash_type`、`args`，Cellbase 与接口特有错误 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-cell-input-lock-scripts-show.md` |
| V1 Cell Input Type Script RPC 正确性 | 核对普通输入引用输出的可空 Type Script、脚本哈希、输入关联、多输入隔离、Cellbase 与 ID 边界；不评审通用 HTTP 契约和 Explorer 脚本元数据 | 主网与测试网的 `GET /api/v1/cell_input_type_scripts/:id`，以及同网络 CKB RPC `get_transaction` | RPC 引用输出 Type Script 的字段与规范哈希、无 Type Script 的空值、Cellbase 与接口特有错误 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-cell-input-type-scripts-show.md` |
| V1 Cell Input Data RPC 正确性 | 核对普通输入引用输出的原始 Data、输入关联、多输入隔离、Cellbase、大小策略与 ID 边界；不评审通用 HTTP 契约 | 主网与测试网的 `GET /api/v1/cell_input_data/:id`，以及同网络 CKB RPC `get_transaction` | RPC 引用输出的完整 Data、空字节串、Cellbase、大小策略与接口特有错误 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-cell-input-data-show.md` |
| V1 Cell Output Lock Script RPC 正确性 | 核对输出 Lock Script、输出索引隔离、Cellbase 输出、已消费输出与 ID 边界；不评审通用 HTTP 契约和 Explorer 脚本元数据 | 主网与测试网的 `GET /api/v1/cell_output_lock_scripts/:id`、用于定位输出的交易展示，以及同网络 CKB RPC `get_transaction` | RPC 同索引输出 Lock Script 的字段、已消费输出可查询性、Cellbase 输出与接口特有错误 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-cell-output-lock-scripts-show.md` |
| V1 Cell Output Type Script RPC 正确性 | 核对可空输出 Type Script、脚本哈希、输出索引隔离、已消费输出与 ID 边界；不评审通用 HTTP 契约和 Explorer 脚本元数据 | 主网与测试网的 `GET /api/v1/cell_output_type_scripts/:id`、用于定位输出的交易展示，以及同网络 CKB RPC `get_transaction` | RPC 同索引输出 Type Script 的字段与规范哈希、无 Type Script 的空值、已消费输出与接口特有错误 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-cell-output-type-scripts-show.md` |
| V1 Cell Output Data RPC 正确性 | 核对输出原始 Data、输出索引隔离、已消费输出、64000 字节边界与 ID 错误；不评审通用 HTTP 契约 | 主网与测试网的 `GET /api/v1/cell_output_data/:id`、用于定位输出的交易展示，以及同网络 CKB RPC `get_transaction` | RPC 同索引完整 Data、空字节串、已消费输出、大小限制与接口特有错误 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v1-cell-output-data-show.md` |
| V2 CKB 交易展示视图 RPC 正确性 | 核对交易输入输出展示项及资产变更是否能从同网络交易和被引用输出确定推导；分页参数通用契约由兼容性领域负责 | `GET /api/v2/ckb_transactions/:id/details`、`GET /api/v2/ckb_transactions/:id/display_inputs`、`GET /api/v2/ckb_transactions/:id/display_outputs`，以及同网络 CKB RPC `get_transaction` | 输入输出身份、顺序、容量、脚本、数据和按地址或资产汇总的变更 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v2-ckb-transaction-views.md` |
| V2 原始交易与容量明细 RPC 正确性 | 核对原始交易结构及普通 Cell 按地址汇总的 CKB 容量净变化；不负责 UDT 等资产汇总 | `GET /api/v2/transactions/:id/raw`、`GET /api/v2/transactions/:id/details`，以及同网络 CKB RPC `get_transaction` | 原始交易字段、输入引用、输出结构及每个地址的 Shannon 容量净变化 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v2-transactions.md` |
| V2 待处理交易 RPC 正确性 | 在有界观测窗口内核对 Explorer 待处理交易与同网络 RPC 交易池；交易池在观测期间变化时将该次事实基准标记为不可用 | `GET /api/v2/pending_transactions`、`GET /api/v2/pending_transactions/count`，以及同网络 CKB RPC 交易池方法 | 交易池成员、可展示交易字段、列表筛选规则、总数及排序字段 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v2-pending-transactions.md` |
| V2 区块扩展 RPC 正确性 | 核对当前 Epoch `E` 至 `max(E-42, 0)` 的最多 43 个 Epoch 编号内由 Cellbase witness 提取的 CKB 节点版本分组，并按 Epoch number 和 Epoch 内索引定位规范链区块；不把 RPC 服务端自身版本当作矿工节点版本，也不评审通用查询参数错误格式 | `GET /api/v2/blocks/ckb_node_versions`、`GET /api/v2/blocks/by_epoch`，以及同网络 CKB RPC `get_epoch_by_number`、`get_block_by_number` 与区块 Cellbase witness | 版本字符串、无法提取版本的 `others` 分组、各组区块数和总数，以及 Epoch 起始高度、索引对应高度、区块哈希和可直接核对的区块字段 | `suites/ckb-explorer-api-rpc-correctness/reviews/chain-data/v2-blocks-extra.md` |
| V1 地址详情 RPC 正确性 | 在公开主网和测试网分别把 CKB 地址或 Lock Script Hash 解析为同一 Lock Script，并用同网络 CKB Indexer、节点 RPC 与可验证推导值核对地址当前状态；同时覆盖 Bitcoin 地址映射到一个或多个 CKB 地址的结果，不跨网络比较，也不评审通用 HTTP 契约、地址交易列表、DAO 交易列表或代币元数据本身的可信度 | 主网与测试网的 `GET /api/v1/addresses/:id`，以及同网络 CKB Indexer `get_cells`、`get_transactions`、节点 RPC `get_transaction` 和必要的 Bitcoin 地址映射事实基准 | 查询地址与 Lock Script 身份、Lock Script Hash 查询分支、Live Cell 容量与占用容量、交易与 Live Cell 数量、DAO 金额、已发布 UDT 账户、特殊地址及 Bitcoin 地址映射的成员和分页 | `suites/ckb-explorer-api-rpc-correctness/reviews/address-dao/v1-addresses-show.md` |
| V1 地址交易流 RPC 正确性 | 在公开主网和测试网分别按 CKB 地址或 Lock Script Hash 核对已提交交易、待处理交易及地址交易 CSV；待处理池在观测期间变化时将该网络事实基准标记为不可用；Bitcoin 地址映射需要独立映射事实基准，不跨网络比较，也不评审地址详情、DAO 专属活动、代币/RGB 注解可信度及通用 HTTP、分页错误和 CSV 下载头契约 | 主网与测试网的 `GET /api/v1/address_transactions/:id`、`GET /api/v1/address_transactions/download_csv`、`GET /api/v1/address_pending_transactions/:id`，以及同网络 CKB Indexer `get_transactions`、节点 RPC `get_transaction`、`get_block`、输入引用的上一笔交易与交易池查询 | 地址参与的已提交及待处理交易集合、去重和顺序，交易哈希、状态、区块字段、输入输出数量与前 10 项预览、地址 Shannon 净变化 `income`，以及 CSV 的过滤成员、交易字段、CKB/UDT 流入流出、余额变化、手续费和 UTC 时间 | `suites/ckb-explorer-api-rpc-correctness/reviews/address-dao/v1-address-transactions.md` |
| V1 地址 Cell RPC 正确性 | 在公开主网和测试网分别按 CKB 地址或 Lock Script Hash 核对当前 Live Cells 及该地址部署的合约 Cell；验证链上 Cell 身份和可推导字段，部署分类以已登记合约的部署 out-point 为锚点；不评审地址详情、已消费 Cells、代币扩展元数据真伪、Bitcoin `bound_status` 映射来源及通用分页和排序错误契约 | 主网与测试网的 `GET /api/v1/address_live_cells/:id`、`GET /api/v1/address_deployed_cells/:id`，以及同网络 CKB Indexer `get_cells`、节点 RPC `get_transaction`、`get_live_cell` 与必要的已登记合约部署 out-point | Live Cell 和部署 Cell 的成员关系、out-point、状态、容量与占用容量、区块高度和时间戳、Lock/Type Script、原始 Output Data、Cell 类型，以及 `fiber`、`multisig`、`deployment` 标签过滤中可由脚本或部署 out-point 验证的成员 | `suites/ckb-explorer-api-rpc-correctness/reviews/address-dao/v1-address-cells.md` |
| 地址 DAO 活动 RPC 正确性 | 在公开主网和测试网分别按地址核对该地址参与的 Nervos DAO 存款、取款请求和利息领取交易，并验证 V1 交易列表与 V2 活动视图来自同一组链上 DAO 事件；不评审全局 DAO 合约统计、全局交易列表、存款人排名及通用分页错误契约 | 主网与测试网的 `GET /api/v1/address_dao_transactions/:id`、`GET /api/v2/dao_events`（参数 `address`），以及同网络 CKB Indexer `get_transactions`、`get_cells` 和节点 RPC `get_transaction`、`get_block`、输入引用的 DAO Cell 与必要的区块头 | 地址 DAO 交易集合、去重和链上顺序，存款、取款第一阶段与利息领取的输入输出关系，交易哈希及区块字段、输入输出预览，以及 V2 返回的地址、当前 DAO 存款容量、平均存款时间、活动列表和总数 | `suites/ckb-explorer-api-rpc-correctness/reviews/address-dao/address-dao-activities.md` |
| V1 Nervos DAO 交易 RPC 正确性 | 在公开主网和测试网分别核对固定合约名 `nervos_dao` 的全局已提交交易列表、单笔 DAO 交易详情和 CSV；覆盖按交易哈希及地址筛选，并从 DAO Cell 的 Type Script、Data、Header Dependency 和输入输出状态区分存款、取款请求与利息领取；不评审 DAO 汇总统计、存款人排名、地址专属活动及通用 HTTP、分页错误和 CSV 下载头契约 | 主网与测试网的 `GET /api/v1/contract_transactions/:id`（当前支持 `id=nervos_dao`）、`GET /api/v1/contract_transactions/download_csv`、`GET /api/v1/dao_contract_transactions/:id`，以及同网络 CKB Indexer `get_transactions`、节点 RPC `get_transaction`、`get_block`、`get_header` 和输入引用的上一笔交易 | 全局 DAO 交易集合、链上顺序及 `tx_hash`/`address_hash` 筛选成员，单笔交易的原始结构、区块归属、输入输出和手续费，非 DAO 交易排除，以及 CSV 的交易哈希、地址、区块、事件方法、金额、手续费和 UTC 时间 | `suites/ckb-explorer-api-rpc-correctness/reviews/address-dao/v1-dao-transactions.md` |
| V1 Nervos DAO 状态与存款人 RPC 正确性 | 在公开主网和测试网分别以链上未消费 DAO 存款/取款 Cell、DAO 事件和区块经济数据核对 `nervos_dao` 汇总状态、前 100 名存款人及存款人 CSV；变化量必须锚定接口所用日统计窗口，不跨网络比较，也不评审 DAO 交易详情、地址活动、估值展示格式及通用 HTTP 和 CSV 下载头契约 | 主网与测试网的 `GET /api/v1/contracts/:id`（当前支持 `id=nervos_dao`）、`GET /api/v1/dao_depositors`、`GET /api/v1/dao_depositors/download_csv`，以及同网络 CKB Indexer `get_cells`、`get_transactions` 和节点 RPC `get_transaction`、`get_block`、`get_header`、`get_block_economic_state` | DAO 总存款、存款人数、未领取和已领取补偿、相邻统计窗口变化量、平均存款时间、挖矿奖励、存款补偿、国库金额及估算 APC；存款人地址、存款容量、平均时间、降序前 100 名，以及按日期或区块边界导出的地址容量汇总 | `suites/ckb-explorer-api-rpc-correctness/reviews/address-dao/v1-dao-state-depositors.md` |
| V2 已验证脚本目录 RPC 正确性 | 在公开主网和测试网分别核对 Explorer 已登记且部署 Cell 仍存活的脚本所声明的链上身份、部署证据和可由链数据重算的统计；`name`、`description`、`rfc`、`website`、`source_url`、`deprecated`、`verified` 等人工维护元数据只检查与选中记录关联，不由 CKB RPC 判断内容真伪；不评审脚本关联交易和 Cells 或通用分页错误契约 | 主网与测试网的 `GET /api/v2/scripts`、`GET /api/v2/scripts/general_info`，以及同网络 CKB Indexer `get_cells`、`get_transactions` 和节点 RPC `get_transaction`、`get_live_cell` | 脚本目录成员，Lock/Type、notes 及排序筛选，Type/Data Code Hash、Hash Type、Dep Type，部署和实际合约 out-point、部署 Cell 状态与容量，以及引用 Cell 容量、交易数、引用 Cell 数等可重算统计 | `suites/ckb-explorer-api-rpc-correctness/reviews/contract-script/v2-scripts-catalog.md` |
| V2 脚本链上关联 RPC 正确性 | 在公开主网和测试网分别按 `code_hash` 与 `hash_type` 核对已登记脚本的部署 Cell、引用该脚本的 Live Cells 和以相应合约 Cell 为 Cell Dependency 的交易；普通模式验证全部依赖关系，`restrict=true` 的实际执行使用分类仅在存在独立执行轨迹时判断；不评审脚本目录人工元数据、地址详情、代币扩展解析和通用分页错误契约 | 主网与测试网的 `GET /api/v2/scripts/ckb_transactions`、`GET /api/v2/scripts/deployed_cells`、`GET /api/v2/scripts/referring_cells`，以及同网络 CKB Indexer `get_cells`、`get_transactions` 和节点 RPC `get_transaction`、`get_live_cell`、必要的区块或执行轨迹查询 | Cell Dependency 交易集合、链上顺序、交易原始及展示字段，部署 Cell 的 out-point、状态、容量与脚本，按 Lock/Type Code Hash 命中的 Live referring Cells，`args` 和 `address_hash` 过滤，以及 Zero Lock 的空部署 Cell 和特殊交易分支 | `suites/ckb-explorer-api-rpc-correctness/reviews/contract-script/v2-scripts-relations.md` |
| V1 UDT 发现、sUDT 目录与持仓分布 RPC 正确性 | 核对名称或 Symbol 搜索、sUDT 列表与已发布详情、交易 CSV 导出及持仓分布，并用同网络 CKB RPC、Indexer 和可验证推导值核对链上身份、供应量与持仓；不负责 xUDT、统一同质化代币、Omiga 专用视图、元数据更新及独立交易历史接口 | `GET /api/v1/udt_queries`、`GET /api/v1/udts`、`GET /api/v1/udts/download_csv`、`GET /api/v1/udts/:id`、`GET /api/v1/udts/:id/holder_allocation` | 搜索成员、sUDT 类型和发布状态、分页与排序、Type Hash 与 Type Script、发行者、供应量和地址/持有人数量、按 Bitcoin 所有者及 Lock Script 合约划分的持仓数，以及日期或区块范围过滤后的交易 CSV 行 | `suites/ckb-explorer-api-rpc-correctness/reviews/token-udt/v1-udt-catalog.md` |
| V1 UDT 元数据与邮箱验证更新正确性 | 在受控状态下核对 UDT 联系邮箱验证、令牌生命周期和元数据更新；覆盖 sUDT 与 xUDT/xUDT-compatible 的不同可更新字段，不负责链上供应量、交易历史、公开目录排序和邮件基础设施本身 | `PATCH /api/v1/udts/:id`、`PUT /api/v1/udts/:id`、`PATCH /api/v1/udt_verifications/:id`、`PUT /api/v1/udt_verifications/:id` | 验证记录创建或刷新、发送 IP、语言和邮件任务、发送频率限制、令牌缺失/错误/过期、首次提交邮箱并发布、后续令牌校验、邮箱保持及不同 UDT 类型允许更新的字段和失败后的状态保持 | `suites/ckb-explorer-api-rpc-correctness/reviews/token-udt/v1-udt-metadata-verification.md` |
| V1 xUDT 与统一同质化代币目录、导出和快照 RPC 正确性 | 核对 xUDT/xUDT-compatible 专用目录与统一 fungible-token 目录、详情、交易导出和指定区块快照；覆盖共享标签、类型、Symbol 和排序规则，不负责 sUDT 专用目录、Omiga 生命周期、元数据写入及地址交易视图 | `GET /api/v1/xudts`、`GET /api/v1/xudts/download_csv`、`GET /api/v1/xudts/snapshot`、`GET /api/v1/xudts/:id`、`GET /api/v1/fungible_tokens`、`GET /api/v1/fungible_tokens/download_csv`、`GET /api/v1/fungible_tokens/:id` | xUDT 类型与 Symbol 过滤、标签交集或并集语义、统一目录的已发布 sUDT/xUDT/xUDT-compatible/SSRI 成员、详情字段与 SSRI 合约 OutPoint、分页排序、过滤后的交易 CSV，以及指定区块时仍存活 Cell 推导的地址余额、余额降序、Bitcoin 所有者合并和 CSV/JSON 快照 | `suites/ckb-explorer-api-rpc-correctness/reviews/token-udt/v1-xudt-fungible-tokens.md` |
| V1 Omiga 铭文生命周期 RPC 正确性 | 核对 Omiga 铭文列表、当前或已关闭详情选择和铭文交易导出，覆盖 minting、closed、rebase_start 及前后代关联；不负责普通 xUDT 目录、通用 UDT 交易导出或元数据验证 | `GET /api/v1/omiga_inscriptions`、`GET /api/v1/omiga_inscriptions/download_csv`、`GET /api/v1/omiga_inscriptions/:id` | 当前铭文集合、被 rebase 后前代的排除、按 UDT Hash 或 Info Type Hash 定位、closed 与最新 rebase 详情、mint 状态排序、铭文参数和供应字段，以及由链上输入输出和数据推导的 mint/rebase_mint CSV 行 | `suites/ckb-explorer-api-rpc-correctness/reviews/token-udt/v1-omiga-inscriptions.md` |
| V1 UDT 交易历史 RPC 正确性 | 核对已发布 UDT 的全局已提交交易历史及地址与 UDT 的交集历史，并用同网络 CKB Indexer、节点 RPC 和输入引用核对成员与展示摘要；不负责 CSV、pending 交易、代币目录元数据及通用地址详情 | `GET /api/v1/udt_transactions/:id`、`GET /api/v1/address_udt_transactions/:id` | 按 Type Hash、可选交易哈希或地址过滤后的去重已提交交易集合、地址与代币同时参与的成员关系、区块时间与交易索引顺序、分页总数，以及交易哈希、区块身份、输入输出和资产预览 | `suites/ckb-explorer-api-rpc-correctness/reviews/token-udt/v1-udt-transactions.md` |
| V2 UDT 时序统计 RPC 正确性 | 核对全部 UDT 的时间桶汇总及单个已发布 UDT 的历史序列，并从同网络链上交易、Cell 金额与持仓事实推导统计值；不负责实时目录字段、百分比变化、元数据更新和通用缓存契约 | `GET /api/v2/udt_hourly_statistics`、`GET /api/v2/udt_hourly_statistics/:id` | 全局按时间戳分组后的交易数与持有人数总和及降序、单 UDT 的金额/交易数/持有人数和升序时间序列、十进制整数的字符串表示、未发布或不存在 UDT 的查询结果 | `suites/ckb-explorer-api-rpc-correctness/reviews/token-udt/v2-udt-hourly-statistics.md` |
| V2 NFT 集合与持有人 RPC 正确性 | 在公开主网和测试网分别用同网络 CKB 节点与 Indexer RPC 核对 m-NFT、NRC-721、Spore/DID 等链上 NFT 集合身份、可从定义 Cell 解码的元数据、活跃 Item/持有人统计及集合过滤成员；CoTA 集合的聚合器专有元数据需单独外部事实基准，不跨网络比较，也不评审通用 HTTP、分页、排序、缓存、远程图片内容、RGB/Bitcoin 关联或标签产生策略本身 | 主网与测试网的 `GET /api/v2/nft/collections`、`GET /api/v2/nft/collections/:id`、`GET /api/v2/nft/collections/:collection_id/holders`，查询参数 `type`、`tags`、`union`、`address_hash`，以及同网络 CKB RPC `get_transaction`、`get_live_cell` 和 CKB Indexer `get_cells`、`get_transactions` | 集合成员与标准、SN/Type Script、创建者地址、定义 Cell 时间戳、链上可解码名称/描述/URI、活跃 Item 数、持有人数、24 小时交易数、类型与标签过滤成员，以及每个持有人当前正常 Item 数量 | `suites/ckb-explorer-api-rpc-correctness/reviews/nft/v2-nft-collections-holders.md` |
| V2 NFT Item RPC 正确性 | 在公开主网和测试网分别从同网络 NFT Type Script、Live Cell 和历史交易推导集合内及全局 Item，核对当前所有者、链上 Cell、状态和集合归属；覆盖十进制与十六进制 Token ID，区分正常与已销毁 Item，CoTA 聚合器状态另需外部事实基准，不评审通用分页、错误格式、本地创建更新时间或远程 metadata/icon 内容 | 主网与测试网的 `GET /api/v2/nft/collections/:collection_id/items`、`GET /api/v2/nft/collections/:collection_id/items/:id`、`GET /api/v2/nft/items`，查询参数 `owner`、`standard`、`token_id`，以及同网络 CKB RPC `get_transaction`、`get_live_cell` 和 CKB Indexer `get_cells`、`get_transactions` | 集合与全局 Item 成员、Token ID 及十进制/十六进制查询等价性、正常/销毁状态、所有者地址、NFT 标准、当前 Cell OutPoint/Data/状态、Type Script、集合嵌套信息、过滤结果和 Token ID 顺序 | `suites/ckb-explorer-api-rpc-correctness/reviews/nft/v2-nft-items.md` |
| V2 NFT 转移与 CSV RPC 正确性 | 在公开主网和测试网分别依据同网络 NFT 输入输出 Cell 重建铸造、普通转移和销毁事件，核对集合级、Item 级和全局历史、嵌套父资源约束、领域过滤及 CSV 行；CoTA 事件的 from/to/action 需聚合器事实基准，不跨网络比较，也不评审通用分页、HTTP/缓存、CSV 响应头与转义、DAS、Bitcoin 或 RGB 专项数据 | 主网与测试网的 `GET /api/v2/nft/collections/:collection_id/transfers`、`GET /api/v2/nft/collections/:collection_id/items/:item_id/transfers`、`GET /api/v2/nft/collections/:collection_id/items/:item_id/transfers/:id`、`GET /api/v2/nft/transfers`、`GET /api/v2/nft/transfers/download_csv`、`GET /api/v2/nft/transfers/:id`，查询参数 `collection_id`、`token_id`、`from`、`to`、`address_hash`、`transfer_action`、`tx_hash` 及 CSV 日期/区块范围，以及同网络 CKB RPC `get_transaction`、`get_block` 和输入引用的上一笔交易 | 集合与 Item 转移成员及父资源归属、mint/normal/destruction 动作、from/to 地址、Token ID 与当前 Item 信息、交易哈希/区块高度/时间戳、过滤和历史顺序，以及 CSV 的交易、方法、地址、手续费、UTC 时间、范围边界和行上限 | `suites/ckb-explorer-api-rpc-correctness/reviews/nft/v2-nft-transfers.md` |
| V2 DAS 反向账户查询正确性 | 对批量 CKB 地址逐项核对 DAS 反向记录结果；有效 DAS 地址以 DAS Indexer 为事实基准，非法地址应被跳过，非 DAS 算法地址或 Indexer 业务错误按接口约定返回空名称；不使用 CKB 节点 RPC 推断账户名，也不评审通用 HTTP、缓存时效、账户注册或所有权变更 | `POST /api/v2/das_accounts`，请求字段 `addresses`，以及对应环境 DAS Indexer `POST /reverse/record` | 输入地址到 DAS account 的映射、非法地址是否缺席、非 DAS 地址的空值、批量请求中各地址结果相互独立 | `suites/ckb-explorer-api-rpc-correctness/reviews/rgb-bitcoin/v2-das-accounts.md` |
| V2 Bitcoin 原始交易批量查询正确性 | 在 CKB 主网对应 Bitcoin mainnet、CKB 测试网对应配置的 signet/testnet 中，按 txid 批量核对 Bitcoin 节点 `getrawtransaction` verbosity 2 的原始结果；覆盖部分 txid 查询失败时的成功子集，不判断 RGB++ 关联、CKB 交易或缓存命中实现 | `POST /api/v2/bitcoin_transactions`，请求字段 `txids`，以及同 Bitcoin 网络 RPC `getrawtransaction(txid, 2)` | 响应按 txid 建立的结果映射、原始 Bitcoin 交易字段和值、请求顺序变化下的成员集合、单项 RPC 错误的缺席及整批上游失败结果 | `suites/ckb-explorer-api-rpc-correctness/reviews/rgb-bitcoin/v2-bitcoin-transactions-query.md` |
| V2 RGB++ 交易跨链关联正确性 | 在主网和测试网分别把 CKB RGB++/BTC Time Lock Cell 与对应 Bitcoin outpoint、OP_RETURN commitment 和 Bitcoin 交易关联起来，核对单笔摘要及 RGB++ 交易列表；使用同网络 CKB RPC、Bitcoin RPC 和确定性 RGB++ 解码/commitment 算法，不负责 Bitcoin 地址持仓视图、全局统计或通用分页错误契约 | `GET /api/v2/ckb_transactions/:id/rgb_digest`、`GET /api/v2/rgb_transactions` 及其 `sort`、`leap_direction`、`page`、`page_size`，以及同网络 CKB RPC `get_transaction`、输入引用交易和 Bitcoin RPC `getrawtransaction` | CKB 与 Bitcoin 交易身份、Bitcoin txid、确认数、OP_RETURN commitment 及验证结果、Bitcoin 地址资产变更、`leap_direction`、`transfer_step`、RGB Cell 净变化、列表成员、筛选、排序和总数 | `suites/ckb-explorer-api-rpc-correctness/reviews/rgb-bitcoin/v2-rgb-transactions.md` |
| V2 Bitcoin 地址关联 RGB 资产正确性 | 从 Bitcoin 地址映射到一个或多个 CKB Lock Script，核对其 live RGB Cell、绑定状态和已发布 UDT/xUDT 余额；同时按合法 RGB++ Lock code hash 查询全局 bound live cells；不评审普通 Bitcoin 余额、已花费 CKB Cell、未发布代币元数据、RGB 交易摘要或通用分页错误 | `GET /api/v2/bitcoin_addresses/:id`、`GET /api/v2/bitcoin_addresses/:id/rgb_cells`、`GET /api/v2/bitcoin_addresses/:id/udt_accounts`、`GET /api/v2/rgb_live_cells`，参数 `page`、`page_size`、`code_hash`，以及同网络 CKB RPC/Indexer 与 Bitcoin RPC | Bitcoin 地址映射的 CKB 地址集合、live Cell 的 bound/unbound 数量、按 Bitcoin outpoint 分组的 RGB Cells、Cell 容量/脚本/数据/类型、已发布 UDT 的 type hash 与整数余额、合法 code hash 下的 bound 非 OP_RETURN live outpoints、分页成员和总数 | `suites/ckb-explorer-api-rpc-correctness/reviews/rgb-bitcoin/v2-bitcoin-address-assets.md` |
| V2 Bitcoin 索引活动统计正确性 | 核对 Explorer 已持久化的半小时 Bitcoin 新地址数和新交易数时间序列及时间升序；统计依据是记录写入时间窗口而非 Bitcoin 区块时间，因此需要可复算的统计快照或索引侧事实基准，不把单次 Bitcoin RPC 链上扫描当作等价 oracle，也不评审调度频率和缓存头 | `GET /api/v2/bitcoin_statistics`，以及按同一半小时窗口保存的 Bitcoin 地址、交易索引统计事实 | 每个时间点的 `timestamp`、`addresses_count`、`transactions_count`，时间顺序、重复时间点和空时间序列 | `suites/ckb-explorer-api-rpc-correctness/reviews/rgb-bitcoin/v2-bitcoin-statistics.md` |
| V2 RGB 资产统计与持有人排名正确性 | 在固定时间截面核对 RGB++ FT、DOB、交易和持有人统计，并对指定 xUDT/xUDT-compatible 合并 Bitcoin 映射持有人与纯 CKB 持有人后计算前十名；金额使用整数原始单位，跨网络数据只在 `network` 字段对应范围内聚合，不评审代币展示元数据、后台调度和通用缓存契约 | `GET /api/v2/rgb_assets_statistics` 及 `network`、`indicators`，`GET /api/v2/rgb_top_holders/:id`，以及同网络 CKB RPC/Indexer、Bitcoin 地址映射和可复算统计快照 | 按时间升序的 `indicator/value/network/created_at_unixtimestamp`、过滤后的成员集合，Top 10 地址、原始整数 amount、`btc`/`ckb` 来源、全量合并后的降序排名和 position ratio | `suites/ckb-explorer-api-rpc-correctness/reviews/rgb-bitcoin/v2-rgb-asset-analytics.md` |
| V1 搜索与节点发现 RPC 正确性 | 在公开主网和测试网分别核对 Explorer 索引高度、CKB 链对象搜索结果及其配置节点信息；`external/stats` 是供外部调用方读取本地 Explorer 索引高度的轻量出口，不是第三方上游统计源；`nets` 的地址、节点 ID 和版本属于 Explorer 所连接节点的实例级信息，只有取得同一配置节点时才作精确结论，任意同网络公共 RPC 只能证明链归属而不能证明节点身份；搜索中的 UDT/NFT、Bitcoin 和 Fiber 结果依赖各自索引或外部数据源，本领域只核对可由 CKB RPC/Indexer 证明的区块、交易、地址和脚本结果，其余类别交给对应专项领域；不评审通用 HTTP 契约 | 主网与测试网的 `GET /api/v1/external/stats/:id`（有效 `id=tip_block_number`）、`GET /api/v1/suggest_queries`（参数 `q`、`filter_by`）、`GET /api/v1/nets`、`GET /api/v1/nets/:id`，以及同网络 CKB RPC `get_tip_header`、`get_block`、`get_transaction`、`local_node_info` 和必要的 CKB Indexer 查询 | Explorer 索引 tip、搜索命中的资源类型与链上身份、聚合搜索成员、节点监听地址、节点 ID、版本和完整 `local_node_info`；外部或节点级事实基准不可用时单独标记，不据此判定索引数据错误 | `suites/ckb-explorer-api-rpc-correctness/reviews/statistics-discovery/v1-search-node-discovery.md` |
| V1/V2 当前统计快照 RPC 正确性 | 核对当前 tip、Epoch、难度、平均出块时间、哈希率、交易量、手续费样本及合约资源统计能否由同网络链数据和 Explorer 已索引 Cells/交易推导；图表中的缓存哈希率、难度和叔块率同时评审时间点、顺序与缓存新鲜度；`blockchain_info` 来自配置 CKB 节点，`maintenance_info`、`flush_cache_info`、重组标记属于运行时缓存状态，合约名称及分类属于 Explorer 元数据，均不得拿任意公共 RPC 的不同实例值作错误结论；待处理手续费依赖动态交易池，观测期变化时事实基准不可用；不评审通用缓存头、参数格式和第三方合约元数据可信度 | 主网与测试网的 `GET /api/v1/statistics`、`GET /api/v1/statistics/:id`、`GET /api/v1/statistic_info_charts`、`GET /api/v2/statistics/transaction_fees`、`GET /api/v2/statistics/contract_resource_distributed` 及可选 `code_hashes`，以及同网络 CKB RPC `get_tip_header`、`get_current_epoch`、`get_block_by_number`、`get_blockchain_info`、`get_transaction`、交易池方法和必要的 CKB Indexer 查询 | 当前 tip 与 Epoch 信息、难度、平均出块时间、估算 Epoch 时间、哈希率、近 24 小时及每分钟交易量、矿工/地址排名、哈希率/难度/叔块率序列、已提交/待处理/按日手续费率，以及每个合约的脚本身份、交易数、近 24 小时交易数、引用容量和地址数 | `suites/ckb-explorer-api-rpc-correctness/reviews/statistics-discovery/current-statistics.md` |
| V1 历史链统计与生成监控 RPC 正确性 | 核对按日、按 Epoch 和分布维度保存的历史统计是否与同网络规范链及已索引 Cells/DAO 事件一致，并单独检查每日统计生成是否更新到昨日；统计值由后台任务、数据库快照、缓存及 `rolling_avg_block_time` 物化视图生成，不把实时 RPC 暂时领先视为历史数据错误；`nodes_distribution` 等源码已标注待移除的 DailyStatistic 列仅按现存持久化语义评审；`GET /api/v1/block_statistics/:id` 在路由中明确标注 `unused`，Gate 2 前须先决定是仅记录退役状态、继续支持现有四个 indicator，还是移出 ACTIVE 范围，不默认把它当受支持接口；监控接口只表示最后一条 DailyStatistic 的日期是否为昨日，不证明各统计字段正确；不评审后台调度可靠性本身和通用 HTTP 契约 | 主网与测试网的 `GET /api/v1/daily_statistics/:id`、`GET /api/v1/block_statistics/:id`、`GET /api/v1/epoch_statistics/:id` 及可选 `limit`、`GET /api/v1/distribution_data/:id`、`GET /api/v2/monitors/daily_statistics`，以及同网络 CKB RPC `get_block_by_number`、`get_block_economic_state`、`get_current_epoch`、必要的 CKB Indexer 查询和 Explorer 已索引 Cell/DAO 事实 | 按时间升序的每日指标及时间戳、按区块或 Epoch 的难度/哈希率/叔块率/时长/长度和极值、地址余额/出块时间/Epoch/节点/矿工分布、滚动平均出块时间、limit 选取的末尾 Epoch 窗口，以及每日生成监控的 `ok`/`error` 状态 | `suites/ckb-explorer-api-rpc-correctness/reviews/statistics-discovery/historical-chain-statistics.md` |
| V1 CKB 供给与货币模型正确性 | 核对当前总供应量、流通供应量以及名义 APC、名义通胀率、实际通胀率曲线的确定性计算；虽然路由名为 `market_data`，源码未调用价格、交易所或其他第三方市场数据源，不覆盖价格、市值、成交量；供应量依赖同网络区块 DAO 字段、DAO 补偿、特定锁定地址余额、硬编码发行/销毁/解锁常量及本地缓存，逐项区分可由 RPC/Indexer 证明的链上量与产品固定假设；货币曲线是固定参数模型而非当前节点 RPC 返回值；不跨网络套用主网锁定地址语义，也不评审通用 HTTP 契约 | 主网与测试网的 `GET /api/v1/market_data`、`GET /api/v1/market_data/:id`、`GET /api/v1/monetary_data/:id`，以及同网络 CKB RPC 区块头 DAO 字段、CKB Indexer Cell 查询和明确记录的发行/销毁/解锁常量 | 总供应量与流通供应量、CKB/Shannon 单位和截断精度、锁定/销毁/DAO 项的计算证据，以及指定月份长度的 APC 和通胀率序列 | `suites/ckb-explorer-api-rpc-correctness/reviews/statistics-discovery/economic-supply-models.md` |
| Portfolio 身份与用户设置正确性 | 验证 CKB 地址、消息、签名与公钥的身份绑定，创建或复用 Portfolio 用户并签发 JWT，确保 PATCH/PUT 更新只作用于 JWT 对应用户；不负责地址资产聚合、通用 HTTP 错误格式或跨用户数据查询 | `POST /api/v2/portfolio/sessions`、`PATCH /api/v2/portfolio/user`、`PUT /api/v2/portfolio/user` | 签名验真结果、用户创建或复用、JWT 中的用户 UUID 与过期时间、JWT 绑定用户的名称变更、无效或过期 JWT 不产生用户状态变更 | `suites/ckb-explorer-api-rpc-correctness/reviews/portfolio/authentication-user.md` |
| Portfolio 地址与资产数据正确性 | 确保 JWT 用户同步的地址集合幂等且保持用户隔离，并以该集合核对 CKB/DAO 汇总、UDT/NFT 账户、已提交交易列表和 CSV；链上容量与交易事实由同网络 CKB RPC/Indexer 验证，代币展示元数据自身的可信度与通用分页、缓存、CSV 下载头由相邻领域负责 | `POST /api/v2/portfolio/addresses`、`GET /api/v2/portfolio/statistics`、`GET /api/v2/portfolio/udt_accounts`、`GET /api/v2/portfolio/ckb_transactions`、`GET /api/v2/portfolio/ckb_transactions/download_csv` | 用户地址成员与去重结果、最新地址一致性、余额/占用容量/DAO 存款与补偿、按 type hash 聚合的 UDT 数量、NFT 成员、用户地址参与的已提交交易集合与净容量变化，以及同一用户范围内的 CSV 行 | `suites/ckb-explorer-api-rpc-correctness/reviews/portfolio/assets-activity.md` |
| Fiber Peer 正确性 | 管理 Explorer 主动监控的 Fiber RPC Peer：创建前调用目标 Fiber RPC `list_channels` 验证连接，按 `peer_id` 幂等登记名称与 RPC 地址并触发异步 Channel 同步；只负责已配置监控 Peer，不负责全网 Graph Node 拓扑 | `GET /api/v2/fiber/peers`、`POST /api/v2/fiber/peers`、`GET /api/v2/fiber/peers/:peer_id` | Peer 成员与分页、RPC 地址合并去重、连接测试结果、创建后的同步触发、READY Channel 数量与本地余额总和，以及 Peer 详情中的 Channel ID、远端 Peer、状态和 flags | `suites/ckb-explorer-api-rpc-correctness/reviews/fiber/peers.md` |
| Fiber Channel 正确性 | 核对已配置监控 Peer 经 Fiber RPC `list_channels` 同步出的单个 Channel 快照及两端 Peer 关系；不负责链上 Graph Channel 的 funding outpoint、开关交易或全网拓扑筛选 | `GET /api/v2/fiber/channels/:channel_id` | Channel 身份、状态名与 flags、本地/远端及 TLC 余额、关闭和更新时间，以及 local/remote Peer 的 ID、名称与 RPC 地址 | `suites/ckb-explorer-api-rpc-correctness/reviews/fiber/channels.md` |
| Fiber 网络图谱正确性 | 核对全网 Fiber RPC `graph_nodes`/`graph_channels` 快照、软删除历史、节点连接关系和 Channel 开关交易，并用同网络 CKB RPC/Indexer 验证 funding outpoint、容量、UDT 与开关交易事实；不负责人工配置 Peer 的本地 Channel 快照或聚合统计时间序列 | `GET /api/v2/fiber/graph_nodes`、`GET /api/v2/fiber/graph_nodes/addresses`、`GET /api/v2/fiber/graph_nodes/:node_id`、`GET /api/v2/fiber/graph_nodes/:node_id/graph_channels`、`GET /api/v2/fiber/graph_nodes/:node_id/transactions`、`GET /api/v2/fiber/graph_channels` | Node 搜索与成员、地址/Peer ID/连接节点、开放 Channel 数和总容量、UDT 配置、软删除时间；Channel 两端、outpoint、链标识、方向更新、费率、容量、UDT、开放/关闭状态与关联交易；地址、状态、日期、资产和金额过滤后的成员、排序及分页 | `suites/ckb-explorer-api-rpc-correctness/reviews/fiber/graph.md` |
| Fiber 统计快照正确性 | 核对 Graph 同步每日生成的 Fiber 网络统计：列表返回最近 7 个完整快照，详情按合法 indicator 返回最近 14 个数据点；不负责 Peer 本地 Channel 统计、缓存响应头或未列入合法 indicator 的详情查询契约 | `GET /api/v2/fiber/statistics`、`GET /api/v2/fiber/statistics/:id` | 按时间倒序的快照范围、节点数、Channel 数、总容量、平均值和中位数、按 CKB/type hash 汇总的流动性及发布时间；合法 indicator 的字段投影和值与同期 Graph 快照推导结果一致 | `suites/ckb-explorer-api-rpc-correctness/reviews/fiber/statistics.md` |
| V2 REST 生成路由审计 | 审计 Rails `resources` 已暴露、但目标 Controller 未定义对应 REST Action 的 22 个入口，确认路由识别目标与实际分发结果；边界外包括已实现的 `raw`、`details`、列表/详情行为、业务数据正确性及通用错误格式 | `GET /api/v2/ckb_transactions`<br>`GET /api/v2/ckb_transactions/:id`<br>`GET /api/v2/transactions`<br>`POST /api/v2/transactions`<br>`GET /api/v2/transactions/new`<br>`GET /api/v2/transactions/:id`<br>`GET /api/v2/transactions/:id/edit`<br>`PATCH /api/v2/transactions/:id`<br>`PUT /api/v2/transactions/:id`<br>`DELETE /api/v2/transactions/:id`<br>`POST /api/v2/nft/collections`<br>`GET /api/v2/nft/collections/new`<br>`GET /api/v2/nft/collections/:id/edit`<br>`PATCH /api/v2/nft/collections/:id`<br>`PUT /api/v2/nft/collections/:id`<br>`DELETE /api/v2/nft/collections/:id`<br>`POST /api/v2/nft/collections/:collection_id/items`<br>`GET /api/v2/nft/collections/:collection_id/items/new`<br>`GET /api/v2/nft/collections/:collection_id/items/:id/edit`<br>`PATCH /api/v2/nft/collections/:collection_id/items/:id`<br>`PUT /api/v2/nft/collections/:collection_id/items/:id`<br>`DELETE /api/v2/nft/collections/:collection_id/items/:id` | 请求是否被路由识别、解析出的 Controller/Action、Action 是否可分发，以及调用方观察到的状态和错误响应 | `suites/ckb-explorer-api-rpc-correctness/reviews/route-audit/v2-rest-generated-routes.md` |
| V2 CoTA Namespace 路由审计 | 审计 `/api/v2/nft/cota` 下 7 个入口的 Rails Controller 常量解析；路由期待 `Api::V2::NFT::Cota::*`，源码实现位于 `Api::V2::Cota::*`；范围止于命名空间可达性和分发结果，CoTA Aggregator 业务结果另行评审 | `GET /api/v2/nft/cota/nft_classes`<br>`GET /api/v2/nft/cota/nft_classes/:nft_class_id/tokens`<br>`GET /api/v2/nft/cota/nft_classes/:nft_class_id/tokens/:id/claimed`<br>`GET /api/v2/nft/cota/nft_classes/:nft_class_id/tokens/:id/sender`<br>`GET /api/v2/nft/cota/transactions`<br>`GET /api/v2/nft/cota/issuers/:id`<br>`GET /api/v2/nft/cota/issuers/:id/minted` | 请求是否被路由识别、路由期待与源码实际定义的 Controller 常量、Action 可达性，以及调用方观察到的缺失 Controller/常量响应 | `suites/ckb-explorer-api-rpc-correctness/reviews/route-audit/v2-cota-namespace-routes.md` |
