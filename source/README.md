# Product Source Workspace

Follow `../AGENTS.md`: reuse the matching submodule checkout under `ckb-explorer-api-rpc/`, or initialize the configured submodule when absent. The test project commits only the submodule gitlink; product source contents remain in the source repository.
