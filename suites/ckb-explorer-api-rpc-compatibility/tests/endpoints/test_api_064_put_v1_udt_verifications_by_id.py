# Generated by scripts/generate_endpoint_tests.py; do not merge API files.
"""API-064 PUT /api/v1/udt_verifications/:id — 更新 UDT 验证."""

from tests.endpoint_support import EndpointCompatibilityTestCase


class TestAPI064(EndpointCompatibilityTestCase):
    API_ID = "API-064"
    TEST_POINT_IDS = ("TP-COMPATIBILITY-API-CONTRACT-001",)

    def test_api_064_compatibility(self) -> None:
        """MOD-TOKENS / ACTIVE / api/v1/udt_verifications#update / TP-COMPATIBILITY-API-CONTRACT-001."""
        data = {'locale': 'en'}

        result = self.request_both(
            method="PUT",
            path="/v1/udt_verifications/:id",
            json_body=data,
        )
        self.assert_responses_compatible(result)
